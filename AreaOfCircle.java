public class AreaOfCircle {
    public static void main(String[] args) {
        int radius = 5;  // hardcoded radius
        int area = 3 * radius * radius;  // π approximated as 3
        System.out.println(area);
    }
}
